# Translation Bridge — Main Screen UI
# Main UI is built directly in app.py (App._build_main) for cohesion,
# since it tightly couples with application state and event handlers.
# This module exists as a placeholder for future refactoring.
